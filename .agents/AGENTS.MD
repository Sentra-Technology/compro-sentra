# Project Context

This repository is the Sentra Technology company website migration workspace.

## Current Direction

- Use Next.js for the production implementation.
- Treat `INSTRUCTION.MD` as the migration handoff and source of truth.
- Port the current website from `.ref/Website.html`, `.ref/website.css`, and the `.ref/components/web-*.jsx` files.
- Preserve the existing Indonesian copy, global CSS class names, visual layout, component order, and assets.

## Workflow Notes

- For non-trivial implementation work, use `.agents/IMPLEMENTATION_PLAN.MD`, `.agents/TASK.MD`, and `.agents/Walkthrough.MD`.
- Plan first, get approval before coding, then execute and verify.
- Ignore old deck/prototype files listed in `INSTRUCTION.MD`.
